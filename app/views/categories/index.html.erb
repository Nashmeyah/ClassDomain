<% if current_user%>
    <h1>Categories:</h1>
    <% @category.each do |category| %>
        <br>
        <li><%= link_to category.name, category_path(category) %></li>
        <br>
    <%end%>

<%else%>
    You must sign in to access lists
<%end%>

